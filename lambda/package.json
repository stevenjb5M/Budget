{
  "name": "budget-planner-lambda",
  "version": "1.0.0",
  "description": "Budget Planner API - Node.js Lambda backend",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "build:tsc": "tsc",
    "dev": "ts-node src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:ui": "vitest --ui",
    "lint": "eslint src --ext .ts",
    "package": "npm run build && zip -r lambda-functions.zip dist"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.500.0",
    "@aws-sdk/lib-dynamodb": "^3.500.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "@types/node": "^20.0.0",
    "@types/uuid": "^9.0.0",
    "@vitest/coverage-v8": "^1.0.0",
    "@vitest/ui": "^1.0.0",
    "esbuild": "^0.19.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.0.0",
    "vitest": "^1.0.0"
  }
}
