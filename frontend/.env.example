# Frontend Environment Configuration

# Development API endpoint (Local Lambda or Docker)
VITE_API_URL_DEV=http://localhost:5000

# Production API endpoint (API Gateway URL - set during build)
VITE_API_URL_PROD=https://your-api-gateway-id.execute-api.us-east-1.amazonaws.com/prod

# Base path for CloudFront deployment (usually just /)
VITE_BASE_PATH=/

# AWS Configuration (for deployment scripts)
AWS_BUCKET_NAME=your-budget-app-bucket
AWS_CLOUDFRONT_DIST_ID=your-cloudfront-distribution-id
AWS_REGION=us-east-1
